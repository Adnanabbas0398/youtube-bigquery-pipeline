# youtube-bigquery-pipeline
SQL data pipeline on Google BigQuery that ingests YouTube trending data, applies multi-step CTE transformations and Z-score anomaly detection to flag suspicious engagement patterns
